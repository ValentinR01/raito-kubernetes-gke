import styled, { css } from "styled-components";

const LabelStyle = styled.label`
  padding-top: 10px;
  padding-bottom: 15px;
  
  ${({}) => {
    return css``;
  }};
`;

export default LabelStyle;
