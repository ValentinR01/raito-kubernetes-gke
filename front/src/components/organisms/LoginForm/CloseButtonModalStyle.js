import styled from 'styled-components';

const CloseButtonModalStyle = styled.button`
    border: none; 
    background: transparent;
    margin-left: 550px;
`;
// transform: 'translate(-50%, -50%);
export default CloseButtonModalStyle;